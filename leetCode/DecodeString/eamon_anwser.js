const decodeString = function(s) {
    //string forEach 돌면서 스택에 넣는다 (parsing 하기)
    // 숫자나오면 숫자에 저장. 
    // [ 나오면 스택에 저장 ] 나올때까지 안에 있는 애들 저장. => recursion 
    // 2[bdf3[bc]] 숫자, 
    s.split("").reverse()
    const recursion = (str) => {
        if(str[0]=== "["){

        }else(str === ){

        }
    }
//     for(const v in s.split("").reverse()){
//         if(v === )
//     }
//  console.log(s.split("").reverse())


};

const s = "3[a2[c]]"

decodeString(s)